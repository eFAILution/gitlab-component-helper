name: Security Hardening

on:
  pull_request:
    branches: [beta, main]
  push:
    branches: [beta, main]
  workflow_dispatch:

permissions:
  contents: read
  security-events: write
  actions: read
  packages: read
  pull-requests: write
  checks: write
  id-token: write

jobs:
  argus-hardening:
    name: Argus Reusable Hardening
    # Pinned to the commit for Argus v1.3.0 for supply-chain safety.
    uses: huntridge-labs/argus/.github/workflows/reusable-security-hardening.yml@b2f9854d5217d4390d276d23938a62575286d2ab
    with:
      scanners: codeql,gitleaks,osv,dependency-review
      enable_code_security: true
      allow_failure: true
      severity_threshold: high
      post_pr_comment: true
    secrets: inherit
